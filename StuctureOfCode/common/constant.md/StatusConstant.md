
```
package com.sky.constant;

/**
 * 状态常量，启用或者禁用
 */
public class StatusConstant {

    //启用
    public static final Integer ENABLE = 1;

    //禁用
    public static final Integer DISABLE = 0;
}
```

很好 👍
这个类是**状态常量（启用/禁用）**的标准写法，我帮你快速点明要点+给出一些工程上的改进建议。

⸻

🧩 这段代码在做什么？
```
public class StatusConstant {

    //启用
    public static final Integer ENABLE = 1;

    //禁用
    public static final Integer DISABLE = 0;
}
```
👉 作用：

在系统中统一表示“启用/禁用”的状态值

常见用法：
```
if (status.equals(StatusConstant.ENABLE)) {
    // do something
}
```
也常出现在：
- 菜品是否起售
- 套餐是否启用
- 账户是否可用
- 配置是否生效
- 后台开关

⸻

🧠 为什么要用常量？

如果不用常量，很多人会这样写：

```if (status == 1) { ... }   // ❌ 魔法数字```

问题：
- 可读性差
- 容易写错
- 以后要改 → 全项目找 1 改很麻烦

⸻

👉 使用常量后的版本：

if (status.equals(StatusConstant.ENABLE)) { ... }   // ✅ 语义清晰

| 好处 | 原因 |
|-------|------|
| 避免魔法数字 | 1/0 等没有语义，不易读 |
| 统一管理 | 常量集中定义，维护简单 |
| IDE 自动提示 | 自动补全，减少拼写错误 |
| 符合规范 | 企业级项目的常见实践 |
| 便于扩展 | 新增更多状态或常量更方便 |



⸻

🧭 工程实践中可以这样升级

✅ 建议 1：使用 int 而不是 Integer

除非必须允许 null
```
public static final int ENABLE = 1;
public static final int DISABLE = 0;
```

⸻

✅ 建议 2：枚举 enum（更推荐在实战中）
```
public enum StatusEnum {
    ENABLE(1),
    DISABLE(0);

    private final int code;

    StatusEnum(int code) {
        this.code = code;
    }

    public int getCode() {
        return code;
    }
}
```

优点：
- 类型安全
- 支持 switch
- 可附加描述名
- 不会传入 2、3、99 这种错误状态

⸻

✅ 建议 3：数据库字段通常 tinyint(1)

| 业务 | ENABLE | DISABLE |
|------|--------|---------|
| 账号 | 1 | 0 |
| 商品 | 1 | 0 |
| 订单开关 | 1 | 0 |

# 常见做法：数据库中用 tinyint(1)

# 1 = 启用 / 有效

# 0 = 禁用 / 无效


⸻

🏁 总结一句话

- 这个类用于统一定义启用/禁用状态，
- ```public static final``` 表示常量不可重新赋值，
- 真实项目中也可升级为 枚举 enum 实现更安全的状态管理。

⸻

如果你愿意，我还能帮你：
- 🔁 改成 enum 版本
- 🧭 讲 enum 和 constant 的区别（高频面试题）
- 🧪 添加数据库字段 + MyBatis 映射示例

  
